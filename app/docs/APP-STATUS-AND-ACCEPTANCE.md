# Mobile App 功能状态与验收

最后更新：2026-08-23

这份文档回答三个问题：App 已经能做什么、哪些只是代码已经写好、下一步怎样判断真的完成。

## 状态怎么读

- **代码已实现**：源码中已经有这项功能，但不代表真实设备上已经通过。
- **自动检查通过**：类型检查、协议测试或构建检查通过。
- **真机已验证**：在 Android 真机上实际操作过，并观察到预期结果。
- **待验收**：还不能对外声称完成，需要按本文步骤操作并记录结果。

## 当前开发优先级

1. **P0：搞清真实数据**。解释远端 21 条与首页/地图可见数量的差异，记录每条未显示记录的原因。
2. **P1：完成正式使用闭环**。实现地图选点、正式记忆详情页、照片查看和 Home 锁定入口。
3. **P2：完成同步操作**。补上正式 Home 的上传/下载入口、结果提示和冲突处理，再做真实云端验收。
4. **P3：补齐照片和性能验收**。覆盖 5/15/30 MB、前后台、长时间地图操作和资源释放。
5. **P4：低优先级能力**。导出/导入加密 JSON 和 Android Keystore/指纹解锁作为备份与便利功能，不阻塞主流程。

## 当前功能表

| 功能 | 用普通话解释 | 当前判断 | 还缺什么 |
| --- | --- | --- | --- |
| 账号入口 | 输入受邀账号和密码登录服务 | 登录入口已实现；你已实测能进入首页 | 21 条远端记忆与本机可见记录的差异仍需定位 |
| 私密空间 | 创建空间，或输入空间密码打开已有空间 | 代码已实现；密码只在本机用于解锁 | 接在真实账号登录后的完整流程中验收 |
| 加密记忆 | 标题、正文、日期、地点和标签保存为密文 | 代码已实现；兼容性和加密测试通过 | 真机完成“保存后退出/锁定/再打开”复测 |
| 首页 | 解锁后看到真实记忆、年份筛选和地图 | 首页和地图壳已实现；当前 21 条中约 17 条可见 | 查清剩余记录是解密失败、无有效坐标、年份筛选还是同步冲突 |
| 地图地点 | 有经纬度的记忆在地图上显示地点 | 现有记忆的 Marker 显示已实现；**新建记忆的用户选点流程未实现** | 增加进入选点、显示选中点、确认/取消和地点名称的流程 |
| 地图详情 | 点击地点后打开对应记忆 | **正式 App 的详情界面未实现**；开发用 `MapVerticalSliceApp` 测试壳另有一个原型详情面板，不能算正式功能 | 编写正式详情页/详情弹层，并接入关闭、照片显示和返回状态 |
| 新建记忆 | 从首页填写内容，选择地点后保存 | 表单和加密保存已实现；**地图选点未实现，因此带地点的新建流程未完成** | 接入正式选点流程，再验证新建内容出现在首页和地图 |
| 照片 | 选择照片，加密保存，打开时解密显示 | 加密和解密函数已实现；正式详情页尚未提供图片查看交互 | 先完成详情页，再做 5/15/30 MB 真机测试及云端 COS 验收 |
| 云端同步 | 上传和下载密文，服务器看不到正文和照片明文 | 解锁时自动下载已接入；**正式 Home 没有上传/同步操作入口** | 先补同步入口、状态和冲突处理，再做真实账号、PostgreSQL、COS 往返验收 |
| 加密备份 | 导出加密 JSON，清空后重新导入 | 代码路径存在，但属于低优先级离线备份；正式 Home 尚无明确入口 | 主流程完成后再决定是否接入正式 UI |
| 指纹解锁 | 用 Android Keystore 保存的设备钥匙解锁 | 代码路径存在，但属于低优先级便利功能 | 主流程和密码解锁稳定后再复测指纹失效回退 |
| 锁定清理 | 锁定后不再显示正文、照片和地图地点 | 清理函数已实现，但当前 Home 尚未接入可见的锁定按钮 | 先接入用户可操作的锁定入口，再做强制验收 |

## 当前唯一验收目标

下一步先关闭下面这一条，不继续增加新功能。注意：当前“手动锁定”还没有 Home 按钮，所以这条流程暂时不能标记通过：

```text
真实账号登录
→ 创建或打开私密空间
→ 查清 21 条远端记忆为何只有 17 条可见
→ 实现新建记忆时的地图选点
→ 实现正式详情页和照片查看
→ 接入 Home 锁定入口并验证敏感数据清理
```

在“记录差异、地图选点、详情页、锁定入口”关闭前，不能称为主流程完成，也不应把同步、备份或指纹解锁当作主流程完成标志。

## 21 条中只有 17 条可见，先看哪一类问题

首页不是把服务器返回的每条记录直接画出来。当前代码会经过四道筛选：

1. **服务器冲突**：同一记忆在本机和服务器使用同一版本号但内容不同，下载会停止并报告冲突。
2. **解密失败**：坏密文会被跳过，其他记录继续显示。
3. **没有有效坐标**：没有地点，或 `lat/lng` 不是有效数字的记忆不会进入地图 Marker。
4. **年份筛选**：选择某一年后，其他年份会暂时从地图中隐藏。

因此“21 条、17 条可见”目前不能直接判断是哪一种。登录后状态诊断中的以下数字才是证据：

```text
远端同步数量
本机解密数量 / 解密失败数量
地点数量 / 有效坐标数量
地图 DTO 数量
```

其中地图 DTO 数量就是实际发送给地图的数量。若要定位具体 4 条，还需要记录它们的记忆 ID、解密错误类型、是否有地点和坐标；当前 UI 只显示汇总数量，尚未显示逐条原因。

另外，地图停稳事件会更新首页状态文字，可能把登录后的同步诊断覆盖掉。因此当前“看不到汇总”本身也不能证明同步没有返回数据。

## 自动检查

在 `memory-recall-mobile` 目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File ..\scripts\sync-fixed-worktrees.ps1
. 'D:\DevTools\Use-DevEnvironment.ps1'
npm run verify
```

`npm run verify` 包含：

1. TypeScript 类型检查。
2. 加密兼容、地图消息、同步动作测试。
3. Expo Doctor 检查。

自动检查通过只能说明代码基础没有回归，不能代替下面的真机验收。

## Redmi 真机验收表

每一行填写日期、APK 文件名、设备、结果和备注。结果只允许写 `通过`、`失败` 或 `未测`。

| 编号 | 操作 | 预期结果 | 结果 |
| --- | --- | --- | --- |
| 1 | 安装当前 ARM64 standalone APK 并启动 | App 不闪退，出现账号入口 | 未测 |
| 2 | 用真实受邀账号登录 | 进入私密空间创建页或密码页 | 未测 |
| 3 | 创建空间或输入正确空间密码 | 进入首页，能看到真实记忆 | 未测 |
| 4 | 查看首页时间轴并切换年份 | 记忆按日期显示，筛选不崩溃 | 未测 |
| 5 | 在地图上查看已有坐标的记忆 | 地图显示对应地点 | 未测 |
| 6 | 新建记忆时进入地图选点 | 当前预期不能通过：正式选点流程尚未实现 | 阻断 |
| 7 | 点击一个已有地点 | 当前预期不能通过：详情界面尚未实现 | 阻断 |
| 8 | 手动锁定 App | 当前预期不能操作：Home 尚无锁定入口 | 阻断 |
| 9 | 再次输入正确空间密码 | 依赖第 8 项，暂不验收 | 阻断 |
| 10 | 输入错误空间密码 | 仍停留在密码页，不显示任何记忆 | 未测 |
| 11 | 新建一条带地图地点的记忆 | 依赖第 6 项，暂不验收 | 阻断 |

## 通过标准

本轮只有同时满足以下条件，才把主流程标记为“完成”：

- `npm run verify` 通过。
- 表中所有非阻断项通过，没有“暂时可以”或人工猜测。
- 21 条记录的差异已能逐条解释，不能只写“有 4 条冲突”。
- 详情页真实显示标题、正文、地点和照片状态。
- Home 有明确可操作的锁定入口，并且锁定后敏感内容确实消失。
- 锁定后不能通过返回、切换年份或点击地图重新看到敏感内容。
- 错误密码不能进入首页。
- 新建的记忆重新锁定、解锁后仍然存在且地点不漂移。
- 记录 APK 的构建提交号和设备信息；不记录账号密码、私密空间密码或照片明文。

## 失败后怎么处理

- 第 1 项失败：先处理 Android 构建或原生启动问题，不测业务功能。
- 第 2-3 项失败：只检查账号、空间解锁和本地密钥流程，不改地图。
- 第 5-6 项失败：只检查坐标、Marker 和详情回传，不改同步协议。
- 第 7-9 项失败：优先处理内存清理和会话生命周期，这是安全阻断。
- 第 10 项失败：检查新建记忆保存和刷新，不扩大到照片或长时性能。
