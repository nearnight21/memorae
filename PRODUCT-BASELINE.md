# Memorae V1 产品基线

> 状态：已收口（V1）
>
> 基线日期：2026-08-28
>
> 规范分支：`main`

本文只固定 Memorae V1 已明确的产品范围、当前状态和验收 Gate，不新增功能，不替代
[`TECHNICAL-BASELINE.md`](TECHNICAL-BASELINE.md)、
[`MEMORY-V1.md`](MEMORY-V1.md) 或
[`docs/SYNC-CRYPTO-FREEZE.md`](docs/SYNC-CRYPTO-FREEZE.md) 中的技术与兼容性约束。

## 1. 判定口径

状态沿用技术基线：

| 状态 | 本文含义 |
| --- | --- |
| `已实现` | 正式产品链路存在，并已有自动检查、构建或实际环境证据。 |
| `部分实现` | 主链路或代码已存在，但 V1 所需的真机、跨端或发布验收尚未闭环。 |
| `Prototype` | 只在垂直切片或测试入口中成立，尚未进入正式产品链路。 |
| `未实现` | 正式产品链路中不存在。 |
| `待验证` | 必须依赖真实账号、真机、真实服务或发布产物才能判定。 |

“代码已实现”“自动检查通过”和“V1 已验收”是三个不同结论。V1 只有在本文全部 Gate
基于同一个候选提交通过后才能标记完成；任一项为“失败”“未测”或“阻断”都不得放行。

## 2. V1 固定范围

Memorae V1 是由 Web、Android App 和 Server 共同组成的端到端加密私人记忆产品。V1 固定包含：

1. 受邀请账号登录；账号密码与私密空间密码相互独立。
2. 私密空间创建、密码解锁、显式锁定，以及锁定后的内存钥匙和敏感界面清理。
3. Memory 的创建、查看、编辑和删除；日期、文字、地点和照片随 Memory 生命周期正确保存。
4. 以地图为主的足迹浏览，以及既定的地区、时间、记忆详情和地点选择链路。
5. Android 国内地图和海外城市级地图能力。
6. `thumbnail`、`preview`、`original` 三档客户端密文照片及按需读取。
7. Web/Android 经 Server、PostgreSQL 和私有 COS 完成密文同步、冲突保护和 tombstone 删除。
8. 加密导出与恢复；导出物不得包含密码、密钥或用户明文。
9. 保持现有生产 Web/API、Android 包名与唯一正式签名的 V1 发布准备和恢复能力。

未列入本表的能力不因本次收口自动进入 V1，也不得在验收时临时扩大 Gate。

### Android 海外城市级地图

Android 海外城市级地图是 V1 必选能力，不是后续增强项。验收结果必须满足：

- 从海外记忆或海外地点选择进入目标城市后，能够在城市级缩放下辨认目标城市和地图上下文，
  并完成记忆 Marker、详情打开、地图选点、地点确认与保存。
- 中文界面优先显示受控中文城市名；城市标签属于地图语义，不伪装成记忆 Marker，
  不参与记忆聚类，也不抢占记忆点击。
- 普通世界或国家层级不持续铺开全局城市标签；进入海外记忆或地点选择上下文后按视野和缩放显示，
  离开上下文后清除。
- 中国大陆地点继续按 GCJ-02 使用，海外地点继续按 WGS-84 使用；不得为通过地图验收批量改写历史坐标。
- 国家级总览、单独坐标点、只有 Marker、只有文字标签，或只能在开发垂直切片中工作，
  均不能替代正式 App 的海外城市级能力。

V1 固定产品结果，不在本文指定 Native AMap、AMap JS WebView 或其他渲染器；实现路线必须以同一套
正式 App Gate 证明结果成立，并继续复用既定的服务端地点搜索与反查边界。

## 3. 当前 V1 状态

| 范围 | 当前状态 | 已确认事实 | 距离 V1 Gate |
| --- | --- | --- | --- |
| Web 正式产品 | `已实现` | 生产 Web 已有账号登录、私密空间、足迹地图、筛选、时间轴、详情和 Memory CRUD；照片按需读取与后台密文同步已接入。 | 候选提交仍须完成生产回归，不以历史通过替代。 |
| Android 账号与私密空间 | `部分实现` | 正式账号入口、私密空间创建/解锁和错误密码保护已接入；Redmi 真机已通过登录、解锁、首页和错误密码检查。 | 显式锁定入口及锁定后敏感数据清理仍未完成正式全链验收。 |
| Android 足迹首页与详情 | `部分实现` | 正式 Home、真实记忆 Marker、地区选择、时间轴和详情代码已接入；真机已显示 19 个有效地点。 | 真实照片、详情返回、长时间操作和候选发布包仍待完整验收。 |
| Android Memory CRUD 与地点选择 | `部分实现` | 创建、详情、编辑、照片管理、地点选择、删除和递增版本 tombstone 已有实现与自动测试。 | 创建到删除的真机全链、第二客户端下载 tombstone、锁定后重开及地点不漂移仍待验收。 |
| Android 国内地图 | `部分实现` | 正式 Home 已接地图与真实地点；既有 ARM64 真机证据确认国内瓦片、Marker/聚类和基础前后台恢复可工作。 | 正式候选包中的选点、详情、持续手势和资源释放仍待验收。 |
| Android 海外城市级地图 | `Prototype` | Native 垂直切片已有 GeoNames 离线城市数据、受控中文名、语义场景开关、密度限制和东京/巴黎/纽约测试；正式 Home 使用的地图链路尚未以同等产品结果闭环。 | 必须进入正式 App，并通过东京、巴黎、纽约和至少一条真实海外记忆的城市级 Gate。 |
| 三档照片 | `部分实现` | Web/Android 已生成并加密三档照片；地图优先 thumbnail、详情优先 preview；COS 短期签名直传直下和摘要校验已实现。 | 真实照片三档跨端往返、5/15/30 MB、前后台、缓存和失败恢复仍待候选包验收。 |
| 密文同步与删除 | `部分实现` | 增量上传计划、记忆先于照片、逐条冲突继续、全量密文列表合并和 tombstone 规则已有实现及回归测试。 | 第二客户端全链和现存 5 条同版本历史冲突仍未关闭。 |
| 加密导出与恢复 | `部分实现` | Android 有加密 JSON 导出、导入和换机解密路径，既有协议与真机兼容证据成立。 | 正式产品可达入口及空白设备恢复完整 Memory、地点和照片仍待验收。 |
| Server 与生产链路 | `已实现` | 账号会话、Vault/Memory 密文、地点代理、PostgreSQL、私有 COS 和同源 HTTPS 已形成生产链路。 | V1 候选仍须完成 Web/Android 公网双向恢复和访问隔离回归。 |
| 生产备份与恢复 | `部分实现` | 已有受保护 PostgreSQL 备份证据和恢复手册。 | 定时备份、加密异地副本、外部告警及隔离环境 PostgreSQL/COS 引用恢复演练尚未全部关闭。 |
| Android 发布准备 | `部分实现` | 包名 `com.memorae.cn`、唯一正式签名、签名配置 fail-closed 和 EAS profiles 已固定；既有 signed release APK 构建证据成立。 | 必须从候选提交生成可独立启动的正式签名包并完成本页全部 Gate；当前未发布。 |

当前结论：**V1 尚未验收通过**。已知阻断集中在 Android 正式全链、海外城市级地图、历史冲突、
加密导出/恢复、真实三档照片、生产恢复闭环和候选发布包；它们是既定收口项，不是新增功能清单。

## 4. V1 验收 Gate

| Gate | 必须通过的结果 | 最低证据 |
| --- | --- | --- |
| G0 基线与兼容性 | `MEMORY-V1`、加密/同步冻结规范、Memory schema、照片和坐标语义未被普通产品改动破坏；`app.json`、`eas.json`、包名和唯一签名保持既定内容与来源。 | 协议兼容测试、配置 diff、无真实 Secret 的提交检查。 |
| G1 仓库门禁 | Web、App、Server 分别通过 `npm run verify`；App 的 Expo Doctor 通过；运行面边界检查和 `git diff --check` 通过。 | 同一候选提交的本地或 CI 完整日志。 |
| G2 Web 生产回归 | 真实受邀账号可登录和解锁；足迹地图、筛选、时间轴、详情、创建、编辑、删除、锁定与按需照片读取可用；`/health` 正常。 | 生产或等价发布环境的脱敏验收记录。 |
| G3 Android 安全入口 | 正式签名 standalone 候选包可独立启动；真实账号登录、私密空间创建/解锁、错误密码拒绝、显式锁定、再次解锁全部通过；锁定后正文、照片、Marker 和内存钥匙不可恢复访问。 | 包文件、候选提交、设备/Android 版本、签名指纹及逐项结果。 |
| G4 Android Memory 全链 | 在真实账号中创建一条带地点和照片的 Memory，查看详情，编辑标题、正文、日期、地点与照片，再删除；版本递增正确，本机 UI 立即更新，重新锁定/解锁后结果不丢失且地点不漂移。 | 单台 Android 真机完整操作记录及脱敏 ID/version 诊断。 |
| G5 Android 地图 | 国内真实地点的浏览、聚类、详情和选点通过；东京、巴黎、纽约及至少一条真实海外记忆通过城市级地图、中文城市上下文、Marker 点击、详情、选点和保存；持续拖动/缩放、前后台、销毁重建无阻断性卡顿、空白、点击穿透或持续内存增长。 | 正式候选包的城市矩阵、长时间真机记录和必要的帧/内存诊断。 |
| G6 跨端同步与历史冲突 | Web 与 Android 经生产等价 PostgreSQL/COS 双向同步同一 Vault；创建、编辑、删除和离线重试成立；第二客户端接收 tombstone 后旧版本不复活；现存同版本历史冲突逐条解释并关闭，远端、本机、可解密、有效坐标和地图可见数量可对账。 | 脱敏记忆 ID、revision、冲突处置和双端结果；不得记录明文或密钥。 |
| G7 照片链路 | Android/Web 双向完成真实 `thumbnail`、`preview`、`original` 直传直下；5/15/30 MB 样本、摘要、过期授权、跨账号隔离、缓存复用、前后台和失败重试通过；照片字节不经过 API，已知 COS 孤儿对象边界如实记录。 | 客户端请求计数、服务端/COS 脱敏元数据及真机结果。 |
| G8 加密导出与恢复 | 正式入口可导出只含 Vault 与密文的包；空白设备导入后只能用原私密空间密码恢复 Memory、地点和照片；错误密码失败，导出物和日志无用户明文、密码或密钥。 | 导出结构检查、空白设备恢复记录和错误密码结果。 |
| G9 生产恢复与发布 | 定时 PostgreSQL 备份、加密异地副本、外部告警和隔离恢复演练通过；恢复后 Web/Android、照片引用、错误密码、失效 token、跨账号访问和 409 冲突回归通过；从同一提交生成正式签名 Android 候选包。 | 脱敏运维记录、恢复演练结果、候选包校验值与签名指纹。 |

### 放行规则

1. G0-G9 必须全部基于同一个候选提交判定为“通过”；历史构建或 Prototype 结果只能作为前置证据。
2. Gate 结果只允许写“通过”“失败”“未测”或“阻断”。“基本可用”“暂时可以”和人工推测不算通过。
3. 自动检查不能替代真机、跨端、真实 COS 或隔离恢复验收；单台真机也不能替代第二客户端 tombstone 验收。
4. Gate 失败时只修复对应既定范围；不得在 V1 验收过程中顺带加入新功能或修改冻结协议。
5. G0-G9 全部通过并记录候选提交、产物和环境后，才可把本文状态改为“V1 已验收”并进入发布。

## 5. 事实来源

- 当前技术事实与风险：[`TECHNICAL-BASELINE.md`](TECHNICAL-BASELINE.md)
- 加密、同步和照片兼容边界：[`docs/SYNC-CRYPTO-FREEZE.md`](docs/SYNC-CRYPTO-FREEZE.md)
- Android 当前状态与真机表：[`app/docs/APP-STATUS-AND-ACCEPTANCE.md`](app/docs/APP-STATUS-AND-ACCEPTANCE.md)
- Android 地图与海外城市标签证据：[`app/docs/AMAP-VERTICAL-SLICE.md`](app/docs/AMAP-VERTICAL-SLICE.md)
- 生产照片、备份和恢复要求：[`server/docs/DEPLOYMENT-RUNBOOK.md`](server/docs/DEPLOYMENT-RUNBOOK.md)
- 环境、签名和 Secret 来源：[`docs/ENVIRONMENT-SECRETS.md`](docs/ENVIRONMENT-SECRETS.md)

后续只在产品事实、当前状态或 Gate 证据发生变化时更新本文。实现存在但尚未完成相应 Gate 的项目，
必须继续保留为 `部分实现`、`Prototype` 或 `待验证`。
